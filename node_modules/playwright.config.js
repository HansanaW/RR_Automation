// playwright.config.js
import { defineConfig } from '@playwright/test';

export default defineConfig({
  testDir: './RRtest', // change this if your tests are in another folder
  reporter: [
    ['list'], // pretty console output
    ['html', { open: 'never' }], // HTML report in playwright-report/
    ['json', { outputFile: 'results.json' }], // JSON output
    ['junit', { outputFile: 'results.xml' }], // JUnit XML for CI/CD
  ],
  use: {
    screenshot: 'only-on-failure', // screenshot on test failure
    video: 'on-first-retry',       // record video if retry needed
    trace: 'retain-on-failure',    // keep Playwright trace on failure
  },
});
